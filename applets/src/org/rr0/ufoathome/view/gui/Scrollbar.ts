import {Component} from "./Component";

/**
 * @deprecated
 */
export class Scrollbar extends Component{
  static HORIZONTAL = 0;

  constructor(dir: number, number: number, samplingRate: number, number2: number, samplingRate2: number) {
    super();
  }
}