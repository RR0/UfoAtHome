import {Image} from "./Image";
import {Color} from "./Color";

/**
 * @deprecated
 */
export class Graphics {
  drawImage(bufferedImage: Image, number: number, number2: number, param4: this, horizonY: number, p: this) {

  }

  drawString(title: String, number: number, number2: number) {

  }

  setColor(inactiveCaptionText: Color) {

  }

  drawLine(number: number, number2: number, number3: number, number4: number) {

  }

  drawRect(number: number, number2: number, number3: number, number4: number) {

  }

  fillRect(number: number, horizonY: number, SKY_WIDTH: number, number2: number) {

  }

  setClip(xStart: number, number: number, SKY_WIDTH: number, SKY_HEIGHT: number) {

  }

  fillOval(x: number, y: number, radius: number, radius2: number) {

  }
}