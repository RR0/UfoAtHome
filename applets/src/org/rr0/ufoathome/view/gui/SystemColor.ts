import {Color} from "./Color";

/**
 * @deprecated
 */
export class SystemColor {
  static control = new Color('lightgray', currentGreen, currentBlue);
  static textText = new Color('black', currentGreen, currentBlue);
  static inactiveCaptionText = new Color('gray', currentGreen, currentBlue) ;
  static controlShadow = new Color('darkGray', currentGreen, currentBlue);
  static blue: Color;
}