/** Contract for `<rr0-ufo-witnesses>`'s user-visible label strings — implemented per language
 * under this directory (`WitnessSelectorMessages_en.ts`, `WitnessSelectorMessages_fr.ts`) and
 * loaded via `loadWitnessSelectorMessages`. */
export interface WitnessSelectorMessages {
  witness: string
}
