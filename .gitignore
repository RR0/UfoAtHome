node_modules/
dist/
dist-embed-ufo/
dist-embed-scene/
dist-embed-sighting/
dist-embed-sighting-editor/
*.log
*.ipr
*.iws
*.iml
.idea/
.vite/
.ai/

# Raw HYG star catalog CSV (~120k rows, ~34MB) — only the filtered/converted output in
# src/assets/stars-mag7.5.bin is checked in. Download from astronexus/HYG-Database's
# hyg/CURRENT/hygdata_v41.csv (see README.md) before running `npm run build:stars`.
scripts/data/

dist-site/
dist-site-lib/

# Generated from the TypeScript by build:schema — never edited, and rebuilt before the site is.
site/generated/
